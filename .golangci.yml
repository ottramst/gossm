# yaml-language-server: $schema=https://golangci-lint.run/jsonschema/golangci.jsonschema.json

version: "2"

linters:
  default: standard
  disable:
    # The codebase has ~32 inherited unchecked-error patterns; re-enable as
    # part of the Go 1.26 modernization (#5).
    - errcheck
